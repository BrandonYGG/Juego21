{
  "name": "game21",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "vite preview --port $PORT --host 0.0.0.0",
    "install-prod": "npm ci --omit=dev"  
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
